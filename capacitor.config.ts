import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.jsantos.customloader',
  appName: 'examen-upax-front',
  webDir: 'www'
};

export default config;
